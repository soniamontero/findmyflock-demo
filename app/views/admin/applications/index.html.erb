<div>
  <h1>Applications</h1>
</div>

<br/>
<p>
  Possible application statuses:
</p>
<small>
  <p>
    <b>Pending:</b> The application hasn't been opened by the company yet.
    <br/>
    <b>Opened:</b> The application has been opened and read by the company.
    <br/>
    <b>Contacted:</b> The company has contacted the developer by email.
    <br/>
    <b>Rejected:</b> The company has rejected the developer's application.
  </p>
</small>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Date created</th>
      <th>Job</th>
      <th>Company</th>
      <th>Dev</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @applications.sort_by(&:created_at).reverse.each do |application| %>
      <tr>
        <td><%= application.match.created_at %></td>
        <td><%= application.match.job.company.name %></td>
        <td><%= application.match.job.title %></td>
        <td><%= application.match.developer.full_name %></td>
        <td><%= application.status %></td>
        <td><%= link_to 'Show', admin_application_path(application) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
